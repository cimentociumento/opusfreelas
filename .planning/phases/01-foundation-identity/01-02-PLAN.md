---
phase: 01-foundation-identity
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/mobile/app/_layout.tsx
  - apps/mobile/src/lib/token-cache.ts
  - apps/mobile/app/(app)/settings.tsx
  - apps/mobile/app/(app)/index.tsx
  - apps/mobile/README.md
  - apps/api/src/index.ts
  - apps/api/src/routes/session-revoke-all.ts
  - apps/api/src/routes/session-revoke-all.test.ts
  - apps/api/.env.example
  - docs/runbooks/clerk-session.md
autonomous: true
requirements:
  - AUTH-02
user_setup:
  - service: clerk
    why: "Session lifetime and inactivity policies (D-04) are configured in Clerk Dashboard"
    dashboard_config:
      - task: "Set session maximum lifetime toward 24 hours (D-04) and review inactivity timeout per Clerk plan limits"
        location: "Clerk Dashboard → Sessions"
must_haves:
  truths:
    - "Clerk session tokens persist using SecureStore-backed token cache (not AsyncStorage)"
    - "Runbook documents how 24h strict session (D-04) maps to Clerk Dashboard settings"
    - "User can trigger sign-out of all sessions from the app UI (D-05) via supported Clerk APIs"
  artifacts:
    - path: "apps/mobile/src/lib/token-cache.ts"
      provides: "SecureStore token cache implementation for Clerk"
    - path: "docs/runbooks/clerk-session.md"
      provides: "D-04/D-05 operational notes"
    - path: "apps/mobile/app/(app)/settings.tsx"
      provides: "Session controls including revoke-all entry point"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "token-cache.ts"
      via: "ClerkProvider tokenCache prop"
      pattern: "tokenCache"
---

<objective>
Ensure AUTH-02: the user remains authenticated across app restarts on the same device by applying Clerk’s recommended **SecureStore** token cache, and operationalize **D-04** (24h strict session) and **D-05** (multi-device with revoke-all) at the product/implementation level.

Purpose: Meet roadmap success criterion #2 (session persists across restarts) while aligning with locked session policy decisions.
Output: Persistent sessions with documented Clerk Dashboard configuration and a settings surface for session revocation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-identity/01-CONTEXT.md
@.planning/phases/01-foundation-identity/01-RESEARCH.md

**Locked decisions:** D-04 (24h session target), D-05 (revoke all sessions option), D-02 (email backup is Clerk account recovery—document under runbook, no custom SMTP in this phase unless Clerk UI already covers it).
</context>

<tasks>

<task type="auto">
  <name>Task 1: SecureStore token cache for Clerk</name>
  <read_first>
    - apps/mobile/app/_layout.tsx
    - https://docs.expo.dev/guides/using-clerk/
  </read_first>
  <files>apps/mobile/src/lib/token-cache.ts, apps/mobile/app/_layout.tsx</files>
  <action>
    Implement a `tokenCache` object compatible with `@clerk/clerk-expo` expectations using `expo-secure-store` for `getToken` / `saveToken` / `clearToken` (follow Expo Clerk guide literally). Wire `tokenCache={secureClerkTokenCache}` on `ClerkProvider` in `apps/mobile/app/_layout.tsx`. Do not store secrets in `AsyncStorage`. Add brief code comment referencing D-04/D-05 vs storage layer (sessions still issued by Clerk; cache is device persistence for AUTH-02).
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>ClerkProvider uses SecureStore token cache; typecheck passes.</done>
  <acceptance_criteria>
    - `grep -q "expo-secure-store" apps/mobile/src/lib/token-cache.ts`
    - `grep -q "tokenCache" apps/mobile/app/_layout.tsx`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Runbook for 24h session policy (D-04)</name>
  <read_first>
    - .planning/phases/01-foundation-identity/01-RESEARCH.md
  </read_first>
  <files>docs/runbooks/clerk-session.md, apps/mobile/README.md</files>
  <action>
    Create `docs/runbooks/clerk-session.md` documenting: (1) target **maximum session lifetime ~24h** per D-04 and where to set it in Clerk Dashboard; (2) note that production limits may depend on Clerk plan (cite internal RESEARCH pitfalls—no legal claims); (3) BR SMS allowlist cross-link for AUTH-01; (4) D-02: user should add **email** as backup factor in Clerk/account settings for recovery. Link this runbook from `apps/mobile/README.md` under "Operação".
  </action>
  <verify>
    <automated>node -e "require('fs').accessSync('docs/runbooks/clerk-session.md')"</automated>
  </verify>
  <done>Runbook file exists and is linked; D-04/D-02/D-05 referenced explicitly by heading or bullet.</done>
  <acceptance_criteria>
    - `grep -q "24" docs/runbooks/clerk-session.md`
    - `grep -q "clerk-session" apps/mobile/README.md`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: API + UI for sign-out and revoke-all sessions (D-05)</name>
  <read_first>
    - apps/api/src/index.ts
    - https://clerk.com/docs/reference/backend/sessions/get-session-list (external)
    - https://clerk.com/docs/reference/backend/sessions/revoke-session (external)
  </read_first>
  <files>apps/api/src/index.ts, apps/api/src/routes/session-revoke-all.ts, apps/api/src/routes/session-revoke-all.test.ts, apps/mobile/app/(app)/settings.tsx, apps/mobile/app/(app)/index.tsx</files>
  <behavior>
    - Test 1: `POST /rpc/session/revoke-all` without `Authorization` header returns 401
    - Test 2: With mocked `authenticateRequest` / JWT, handler calls Clerk revoke APIs (mock `@clerk/backend` client) or returns 200 when session list empty
  </behavior>
  <action>
    Extend `apps/api` (from 01-01) with **RPC-style** route `POST /rpc/session/revoke-all` (D-08 naming) mounted in `apps/api/src/index.ts`. Implement using `@clerk/backend` `createClerkClient({ secretKey: process.env.CLERK_SECRET_KEY })`: authenticate incoming request via `authenticateRequest` (same pattern as 01-RESEARCH); list sessions for the **current user** and **revoke every session except none** (revoke all including current device per D-05 button label—product may sign user out immediately after). Use `sessions.getSessionList({ userId })` and `sessions.revokeSession(sessionId)` per Clerk Backend reference. Add `apps/api/src/routes/session-revoke-all.test.ts` with Vitest mocks for Clerk client—**no network**.

    In `apps/mobile/app/(app)/settings.tsx`: **Sair** calls `useAuth().signOut()`; **Encerrar todas as sessões** obtains token via `getToken()` from Clerk and `fetch` to `${EXPO_PUBLIC_API_URL}/rpc/session/revoke-all` with `Authorization: Bearer`. Add `EXPO_PUBLIC_API_URL` to `apps/mobile/.env.example`. Add a link/button on `apps/mobile/app/(app)/index.tsx` to navigate to `/settings`.

    Env: `CLERK_SECRET_KEY` required on API (document in root `README.md` and `apps/api/.env.example`).
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run</automated>
  </verify>
  <done>Revoke-all works end-to-end in dev when env keys set; tests cover auth gate; settings reachable from home.</done>
  <acceptance_criteria>
    - `grep -q "session-revoke-all" apps/api/src/index.ts`
    - `grep -q "revoke-all" apps/mobile/app/(app)/settings.tsx`
    - `grep -q "EXPO_PUBLIC_API_URL" apps/mobile/.env.example`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device secure storage | Token cache material protected by OS keystore via SecureStore |
| Clerk Dashboard | Session lifetime changes are administrative; not app-user accessible |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | Tokens in AsyncStorage | mitigate | SecureStore-only token cache; grep CI for AsyncStorage misuse in token cache |
| T-02-02 | Elevation | Revoke-all calls without auth | mitigate | Any revoke-all API must require Clerk session JWT (implemented with server in 01-04) |
| T-02-03 | Denial of service | Accidental sign-out UX | accept | User-initiated action; confirm optional in later UX phase |

</threat_model>

<verification>
Automated: mobile `tsc --noEmit`. Manual sampling per `01-VALIDATION.md`: cold restart retains session when Clerk session valid.
</verification>

<success_criteria>
AUTH-02: persisted session across restarts on device (verified manually + SecureStore wiring); D-04/D-05 documented and surfaced in UI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-02-SUMMARY.md`
</output>
