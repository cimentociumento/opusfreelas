---
phase: 01-foundation-identity
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/tsconfig.json
  - apps/mobile/babel.config.js
  - apps/mobile/metro.config.js
  - apps/mobile/.gitignore
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/index.tsx
  - apps/mobile/README.md
autonomous: true
requirements:
  - AUTH-01
user_setup: []
must_haves:
  truths:
    - "Expo app uses SDK 55 and expo-router"
    - "App launches to a visible home screen (placeholder)"
  artifacts:
    - path: apps/mobile/app/_layout.tsx
      provides: "Root layout for future ClerkProvider"
    - path: apps/mobile/package.json
      provides: "expo ~55 and expo-router"
  key_links:
    - from: apps/mobile/package.json
      to: apps/mobile/app/_layout.tsx
      via: "expo-router"
---

<objective>
Create `apps/mobile` as an **Expo SDK 55** app with **expo-router**, providing the shell for plan **01-05** (Clerk phone OTP).

Purpose: Multiplatform baseline per AGENTS.md.
Output: Runnable Expo project from `apps/mobile`, placeholder UI, README.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@AGENTS.md (Expo SDK 55, expo-router)

**Pin:** `expo@~55.0.0`. Package name `@amauc/mobile`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/mobile (Expo 55 + expo-router)</name>
  <read_first>
    - AGENTS.md (Expo SDK 55)
    - c:\opusfreelas\package.json
  </read_first>
  <files>apps/mobile/package.json, apps/mobile/app.json, apps/mobile/tsconfig.json, apps/mobile/babel.config.js, apps/mobile/metro.config.js, apps/mobile/.gitignore, apps/mobile/app/_layout.tsx, apps/mobile/app/index.tsx</files>
  <action>
    Scaffold Expo SDK **55** with **expo-router**. Set `name` to `@amauc/mobile`. Include `app/_layout.tsx` and `app/index.tsx`. Add `.gitignore` for `.expo`, `node_modules`. Run `pnpm install` from repo root after adding the package. **Do not** add `@clerk/clerk-expo` in this plan.
  </action>
  <acceptance_criteria>
    - `rg -n "\"expo\":\\s*\"~55" apps/mobile/package.json` matches
    - `rg -n "expo-router" apps/mobile/package.json` matches
    - `Test-Path apps/mobile/app/_layout.tsx` is true (PowerShell)
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; rg -n "expo-router" apps/mobile/package.json; rg -n "~55" apps/mobile/package.json</automated>
  </verify>
  <done>Mobile package exists and is workspace-linked</done>
</task>

<task type="auto">
  <name>Task 2: Placeholder home + README</name>
  <read_first>
    - apps/mobile/app/_layout.tsx
  </read_first>
  <files>apps/mobile/app/index.tsx, apps/mobile/README.md</files>
  <action>
    `app/index.tsx`: title **Opus Freelas**, subtitle noting Clerk auth comes in plan 01-05. `README.md`: how to run `pnpm --filter @amauc/mobile start` or `npx expo start` from `apps/mobile`.
  </action>
  <acceptance_criteria>
    - `rg -n "Opus Freelas" apps/mobile/app/index.tsx` matches
    - `rg -n "expo start|pnpm" apps/mobile/README.md` matches
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; rg -n "Opus Freelas" apps/mobile/app/index.tsx</automated>
  </verify>
  <done>Placeholder UI + run docs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev client | No API keys in this plan |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | Placeholder UI | accept | No PII |
| T-02-02 | T | Template drift | mitigate | Pin `expo@~55.0.0` |
</threat_model>

<verification>
Root `pnpm install`; grep SDK 55 and expo-router in `apps/mobile/package.json`.
</verification>

<success_criteria>
Expo SDK 55 + expo-router; home screen shows product title; README documents dev server.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-02-SUMMARY.md`
</output>
