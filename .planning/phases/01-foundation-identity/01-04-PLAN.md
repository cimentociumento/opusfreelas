---
phase: 01-foundation-identity
plan: 04
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-03"
files_modified:
  - apps/api/package.json
  - apps/api/src/middleware/clerk.ts
  - apps/api/src/rpc/router.ts
  - apps/api/src/rpc/identity.ts
  - apps/api/src/sessions/revoke.ts
  - apps/api/src/index.ts
  - apps/api/src/authz/roles.test.ts
  - apps/api/.env.example
autonomous: true
requirements:
  - AUTH-03
user_setup:
  - service: clerk
    why: "JWT verification and session APIs (D-05)"
    env_vars:
      - name: CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard → API keys"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard → API keys"
      - name: CLERK_JWT_KEY
        source: "Optional PEM for networkless verify"
      - name: CLERK_AUTHORIZED_PARTIES
        source: "Comma-separated azp: Expo scheme, localhost"
    dashboard_config:
      - task: "Session max lifetime ~24h (D-04); phone + backup email (D-02)"
        location: "Clerk Dashboard"
must_haves:
  truths:
    - "401 without Bearer token on protected RPC"
    - "403 when provider-only RPC and profile.is_provider is false"
    - "Session revoke path calls Clerk session list/revoke (D-05)"
  artifacts:
    - path: apps/api/src/middleware/clerk.ts
      provides: "Clerk JWT verification + authorizedParties"
    - path: apps/api/src/rpc/identity.ts
      provides: "Identity RPC"
    - path: apps/api/src/authz/roles.test.ts
      provides: "AUTH-03 unit tests"
    - path: apps/api/src/sessions/revoke.ts
      provides: "Revoke other sessions"
  key_links:
    - from: apps/api/src/middleware/clerk.ts
      to: Authorization Bearer
      via: authenticateRequest
---

<objective>
Implement Hono + `@clerk/backend` middleware, **RPC-style** `POST /rpc` with Zod (**D-08**), **Supabase** profile read/update using service role + `clerk_user_id` filter (or user JWT per team choice — document in SUMMARY), **provider-only** stub returning 403 when `is_provider` is false, and **session revoke others** via Clerk Backend API (**D-05**). Vitest covers 401/403 and revoke with mocks.

Purpose: AUTH-03 backend enforcement (D-07). Observability is plan **01-06** (avoid duplicate OTel here).
Output: Protected routes, tests green, `.env.example` updated.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation-identity/01-RESEARCH.md

**Libraries:** `@clerk/backend@^3.2.0`, `@supabase/supabase-js@^2.49.0`, `zod@^3.24.0`.

**authorizedParties:** From `process.env.CLERK_AUTHORIZED_PARTIES.split(',')`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clerk middleware + RPC envelope</name>
  <read_first>
    - apps/api/src/index.ts
    - packages/shared/src/identity/schemas.ts
  </read_first>
  <files>apps/api/package.json, apps/api/src/middleware/clerk.ts, apps/api/src/rpc/router.ts, apps/api/src/index.ts, apps/api/.env.example</files>
  <behavior>
    - Unauthenticated POST /rpc returns 401
    - Unknown procedure returns 400 with Zod-discriminated error
  </behavior>
  <action>
    Add `@clerk/backend`, `@supabase/supabase-js`. Implement `middleware/clerk.ts` using `authenticateRequest` with **authorizedParties**. Mount `POST /rpc` in `rpc/router.ts` with body `{ procedure: string, input: unknown }` and Zod union for `identity.getProfile`, `identity.updateRoles`. Wire in `index.ts`. Extend `.env.example`: `CLERK_*`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`. Mock Clerk in tests.
  </action>
  <acceptance_criteria>
    - `rg -n "authenticateRequest|authorizedParties" apps/api/src/middleware/clerk.ts` matches
    - `pnpm vitest run` in apps/api exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas\apps\api; pnpm vitest run</automated>
  </verify>
  <done>Middleware + RPC envelope tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Identity handlers + provider-only guard tests</name>
  <read_first>
    - packages/shared/src/identity/roles.ts
    - supabase/migrations/20260409000000_init_identity.sql
  </read_first>
  <files>apps/api/src/rpc/identity.ts, apps/api/src/authz/roles.test.ts</files>
  <behavior>
    - `identity.providerOnlyPing` returns 403 when `is_provider` false
    - Returns 200 when true (mocked DB)
  </behavior>
  <action>
    Implement handlers using Supabase service client: select/upsert `profiles` by `clerk_user_id === userId`. Add `identity.providerOnlyPing`. Mock Supabase in Vitest. Use `@amauc/shared` `assertCanActAs` where applicable.
  </action>
  <acceptance_criteria>
    - `rg -n "403|providerOnlyPing" apps/api/src/authz/roles.test.ts` matches
    - `pnpm vitest run src/authz/roles.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas\apps\api; pnpm vitest run src/authz/roles.test.ts</automated>
  </verify>
  <done>AUTH-03 covered by unit tests</done>
</task>

<task type="auto">
  <name>Task 3: Revoke other sessions (D-05)</name>
  <read_first>
    - https://clerk.com/docs/reference/backend/sessions/get-session-list
  </read_first>
  <files>apps/api/src/sessions/revoke.ts, apps/api/src/index.ts</files>
  <action>
    Add route `POST /sessions/revoke-others` (or RPC `session.revokeOthers`) listing sessions and revoking all except `keepSessionId` from body (Zod). Mock Clerk `sessions` in tests. Comment references D-04 Dashboard session policy.
  </action>
  <acceptance_criteria>
    - `rg -n "revokeSession|getSessionList" apps/api/src/sessions/revoke.ts` matches
    - `pnpm vitest run` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas\apps\api; pnpm vitest run</automated>
  </verify>
  <done>D-05 API path implemented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Bearer JWT verified before RPC |
| API → DB | Service role only server-side |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | S | Bearer token | mitigate | Clerk verify; 401 |
| T-04-02 | E | Role escalation | mitigate | DB flags + tests |
| T-04-03 | S | Token replay | mitigate | authorizedParties |
</threat_model>

<verification>
`pnpm vitest run` in `apps/api`.
</verification>

<success_criteria>
401/403 behaviors tested; revoke route implemented; AUTH-03 enforced on API.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-04-SUMMARY.md`
</output>
