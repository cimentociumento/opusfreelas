---
phase: 01-foundation-identity
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260409000000_init_identity.sql
  - .env.example
  - docs/runbooks/supabase-clerk.md
  - scripts/verify-supabase-push-evidence.mjs
autonomous: false
requirements:
  - AUTH-03
user_setup:
  - service: supabase
    why: "Apply SQL migrations (D-01 BaaS)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (non-interactive CLI)"
    dashboard_config:
      - task: "Clerk + Supabase third-party JWT integration"
        location: "Supabase Authentication providers; Clerk integration wizard"
must_haves:
  truths:
    - "`public.profiles` exists with `clerk_user_id` and contractor/provider flags"
    - "RLS uses `auth.jwt()->>'sub'` for Clerk"
    - "`supabase db push` applied with recorded evidence"
  artifacts:
    - path: supabase/migrations/20260409000000_init_identity.sql
      provides: "profiles + RLS"
    - path: supabase/config.toml
      provides: "Supabase CLI config"
  key_links:
    - from: supabase/migrations/*.sql
      to: Hosted or local Postgres
      via: supabase db push
---

<objective>
Add Supabase CLI layout and SQL migration for `public.profiles` keyed by Clerk `sub`, with RLS per Supabase+Clerk docs. **Blocking:** run **`supabase db push`** after migrations are committed.

Purpose: AUTH-03 server-side role state in Postgres (D-07); Supabase as primary BaaS (D-01).
Output: Versioned SQL, pushed schema, runbook.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation-identity/01-RESEARCH.md

RLS policies must use `(auth.jwt()->>'sub')` — not `auth.uid()` — for Clerk JWTs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: supabase/config.toml + migration profiles + RLS</name>
  <read_first>
    - .planning/phases/01-foundation-identity/01-CONTEXT.md (D-06)
  </read_first>
  <files>supabase/config.toml, supabase/migrations/20260409000000_init_identity.sql, .env.example, scripts/verify-supabase-push-evidence.mjs</files>
  <action>
    `supabase init` or hand-author `supabase/config.toml`. Migration `20260409000000_init_identity.sql`: enable `pgcrypto` if using `gen_random_uuid`; `CREATE TABLE public.profiles` with `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `clerk_user_id text NOT NULL UNIQUE`, `is_contractor boolean NOT NULL DEFAULT true`, `is_provider boolean NOT NULL DEFAULT false`, timestamps; index on `clerk_user_id`; enable RLS; policies SELECT/UPDATE/INSERT where `clerk_user_id = (auth.jwt()->>'sub')` for authenticated role per integration docs. Append placeholders to root `.env.example`: `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`. Ensure `scripts/verify-supabase-push-evidence.mjs` exists (Nyquist pairing for Task 3 — checks `01-03-SUMMARY.md` for push evidence).
  </action>
  <acceptance_criteria>
    - `rg -n "auth\\.jwt\\(\\)->>'sub'" supabase/migrations/` matches
    - `rg -n "create table public\\.profiles" supabase/migrations/` matches (case insensitive)
    - `node scripts/verify-supabase-push-evidence.mjs` exists and runs (exits 1 until SUMMARY is written — expected before Task 3 completes)
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; rg -n "profiles|auth.jwt" supabase/migrations/</automated>
  </verify>
  <done>Migration SQL committed</done>
</task>

<task type="auto">
  <name>Task 2: Runbook Clerk ↔ Supabase</name>
  <read_first>
    - docs/runbooks/supabase-clerk.md (create)
  </read_first>
  <files>docs/runbooks/supabase-clerk.md</files>
  <action>
    Document Dashboard steps: Clerk–Supabase connect, JWT issuer, BR +55 SMS allowlist pointer (AUTH-01 ops), backup email (D-02). Link env vars from `.env.example`.
  </action>
  <acceptance_criteria>
    - `rg -n "Clerk|Supabase|sub" docs/runbooks/supabase-clerk.md` matches
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; rg -n "sub" docs/runbooks/supabase-clerk.md</automated>
  </verify>
  <done>Runbook exists</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 [BLOCKING]: supabase db push</name>
  <read_first>
    - supabase/migrations/20260409000000_init_identity.sql
  </read_first>
  <files>— (CLI; evidence in 01-03-SUMMARY.md)</files>
  <action>
    Install Supabase CLI. Set **`SUPABASE_ACCESS_TOKEN`** for non-TTY. `supabase link --project-ref $SUPABASE_PROJECT_REF` if needed. Run **`supabase db push`**. If cloud unavailable, `supabase start` locally then `supabase db push` and record in SUMMARY. Verify `profiles` exists via SQL query; paste evidence into `01-03-SUMMARY.md`.
  </action>
  <acceptance_criteria>
    - SUMMARY documents exit code 0 for `supabase db push`
    - Verification query shows `profiles` in `information_schema.tables` or equivalent
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-supabase-push-evidence.mjs</automated>
  </verify>
  <done>Schema applied; evidence recorded</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB migration | Applied with elevated privileges via Supabase CLI |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | E | RLS misconfiguration | mitigate | Policies on `sub`; tests in 01-04 |
| T-03-02 | I | Service role in repo | mitigate | Only `.env.example`; no secrets in git |
| T-03-03 | T | SQL tampering | mitigate | Versioned migrations; review |
</threat_model>

<verification>
Grep migration for RLS + `profiles`; read `01-03-SUMMARY.md` for push evidence.
</verification>

<success_criteria>
`profiles` exists on target DB; RLS uses Clerk `sub`; blocking push evidenced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-03-SUMMARY.md`
</output>
