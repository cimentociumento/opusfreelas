---
phase: 01-foundation-identity
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260409120000_profiles_and_rls.sql
  - supabase/seed.sql
  - README.md
  - docs/runbooks/supabase-clerk-rls.md
autonomous: false
requirements:
  - AUTH-03
user_setup:
  - service: supabase
    why: "Hosted Postgres + RLS for profiles keyed by Clerk JWT sub (AUTH-03)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (for non-interactive `supabase link` / CI)"
      - name: SUPABASE_PROJECT_REF
        source: "Supabase project settings → Reference ID"
      - name: SUPABASE_URL
        source: "Supabase project → API → Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase project → API → anon public key"
    dashboard_config:
      - task: "Enable Clerk as third-party auth provider per Supabase + Clerk integration docs"
        location: "Supabase Dashboard → Authentication → Providers"
must_haves:
  truths:
    - "SQL migration defines `public.profiles` with contractor/provider flags and RLS using `auth.jwt()->>'sub'`"
    - "Schema is applied to the linked Supabase project via `supabase db push` ([BLOCKING] task)"
    - "Runbook explains Clerk JWT + RLS expectations for ASVS L1"
  artifacts:
    - path: "supabase/migrations/20260409120000_profiles_and_rls.sql"
      provides: "profiles table + policies"
    - path: "docs/runbooks/supabase-clerk-rls.md"
      provides: "Operator verification steps"
  key_links:
    - from: "auth.jwt()->>'sub'"
      to: "Clerk user id"
      via: "RLS policies"
      pattern: "auth\\.jwt\\(\\)"
---

<objective>
Deliver AUTH-03 data layer: **Supabase** Postgres schema and **RLS** so contractor/provider separation is enforceable server-side (D-07), keyed to **Clerk `sub`** via Supabase third-party JWT (D-01). Apply migrations to a real linked project using **`supabase db push`** ([BLOCKING]).

Purpose: Give plan `01-04` a real database contract for profile reads/writes and authorization tests.
Output: Versioned SQL migration, pushed schema, operator runbook.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-identity/01-RESEARCH.md
@.planning/phases/01-foundation-identity/01-CONTEXT.md
@packages/shared/src/roles.ts

**RLS rule:** Use `(auth.jwt()->>'sub')` for Clerk user id—not `auth.uid()` (per Supabase + Clerk docs).

**Firebase (D-01 suporte):** Do not add Firebase Auth; optional `docs/firebase-support.md` notes FCM/Analytics as future wiring only (A1 assumption).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase migration — profiles + RLS for Clerk JWT</name>
  <read_first>
    - packages/shared/src/roles.ts
    - https://supabase.com/docs/guides/auth/third-party/clerk (external)
  </read_first>
  <files>supabase/config.toml, supabase/migrations/20260409120000_profiles_and_rls.sql, supabase/seed.sql, docs/runbooks/supabase-clerk-rls.md</files>
  <action>
    Initialize Supabase CLI layout: `supabase/config.toml` (default local settings acceptable). Create migration `supabase/migrations/20260409120000_profiles_and_rls.sql` that:

    - Creates `public.profiles` with primary key `id text primary key` storing **Clerk user id** (`sub` as text), columns `is_contractor boolean not null default false`, `is_provider boolean not null default false`, `created_at timestamptz default now()`, `updated_at timestamptz default now()`, constraint that at least one of `is_contractor` / `is_provider` may be true (both may be true per D-06—do **not** add mutual exclusivity constraint).

    - Enables RLS on `public.profiles`.

    - Adds policies: **SELECT** and **UPDATE** for `authenticated` role where `(auth.jwt()->>'sub') = id`; **INSERT** for own row only with same predicate.

    - Grants appropriate table privileges to `authenticated`.

    Optional `supabase/seed.sql` empty or commented. Document in `docs/runbooks/supabase-clerk-rls.md` how to validate JWT claims in SQL editor (example `select auth.jwt();`) and link Clerk + Supabase dashboard steps.

    Pin naming to `packages/shared` role semantics—if mismatch, update shared types in coordination (file ownership: prefer **no** edit to `packages/shared` here unless 01-01 types are wrong; if edit required, list `packages/shared/src/roles.ts` in `files_modified` and add second line to this task).

  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('supabase/migrations/20260409120000_profiles_and_rls.sql','utf8'); if(!s.includes('auth.jwt()'))process.exit(1); console.log('ok')"</automated>
  </verify>
  <done>Migration file contains `profiles`, `enable row level security`, and `auth.jwt()` predicate; runbook exists.</done>
  <acceptance_criteria>
    - `grep -q "auth.jwt()" supabase/migrations/20260409120000_profiles_and_rls.sql`
    - `grep -q "profiles" supabase/migrations/20260409120000_profiles_and_rls.sql`
    - `test -f docs/runbooks/supabase-clerk-rls.md`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply schema — supabase db push</name>
  <read_first>
    - supabase/migrations/20260409120000_profiles_and_rls.sql
    - README.md
    - Supabase CLI docs for db push (external)
  </read_first>
  <files>README.md, .env.example</files>
  <action>
    **BLOCKING:** Link the local repo to the Supabase project: `supabase link --project-ref $SUPABASE_PROJECT_REF` using non-interactive auth via `SUPABASE_ACCESS_TOKEN` (document in root `README.md` and `.env.example`). Then run **`supabase db push`** to apply pending migrations to the linked remote database. If the CLI prompts in TTY, prefer env-driven link documented at https://supabase.com/docs/guides/cli (access token). Record exact commands executed in plan SUMMARY (executor). On Windows PowerShell, document equivalent `$env:SUPABASE_ACCESS_TOKEN` usage.

    **Do not** mark phase verification complete without successful push or documented blocker + human resume steps.

    Set `autonomous: false` at plan level because this task may require human-provided token or project ref on first run.

  </action>
  <verify>
    <automated>npx supabase migration list</automated>
  </verify>
  <done>Remote database shows applied migration; `supabase migration list` reports synced state.</done>
  <acceptance_criteria>
    - `grep -q "supabase db push" README.md` OR `grep -q "db push" README.md`
    - `grep -q "SUPABASE_ACCESS_TOKEN" README.md`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Firebase support placeholder (D-01) + README cross-links</name>
  <read_first>
    - .planning/phases/01-foundation-identity/01-CONTEXT.md
    - README.md
  </read_first>
  <files>docs/firebase-support.md, README.md</files>
  <action>
    Add `docs/firebase-support.md` stating D-01 **Firebase (suporte)** is **not** an identity provider: future use for FCM / Analytics / Remote Config only; no `firebase/auth` integration in V1. Link from root `README.md` under "Stack notes". This satisfies stack completeness without scope creep.

  </action>
  <verify>
    <automated>node -e "require('fs').accessSync('docs/firebase-support.md')"</automated>
  </verify>
  <done>Doc exists; README links; no Firebase Auth code added.</done>
  <acceptance_criteria>
    - `grep -qi "FCM\\|Analytics" docs/firebase-support.md`
    - `grep -q "firebase-support" README.md`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase PostgREST + RLS | Row access depends on validated JWT claims; anon key alone must not bypass RLS |
| Operator CLI | `SUPABASE_ACCESS_TOKEN` is highly sensitive |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation | Mis-keyed RLS (wrong JWT claim) | mitigate | Policies use `auth.jwt()->>'sub'`; migration reviewed; runbook lists SQL checks |
| T-03-02 | Information disclosure | Public anon reads PII | mitigate | No broad `USING true` policies on profiles; deny-by-default with RLS |
| T-03-03 | Tampering | Migration drift local vs remote | mitigate | `supabase db push` + migration list in verify |
| T-03-04 | Repudiation | Who applied migration | accept | Phase 1 local/CI; audit handled by Supabase logs later |

</threat_model>

<verification>
`supabase migration list` shows applied migration; manual: run SQL `select * from pg_policies where tablename = 'profiles';` in Supabase SQL editor.
</verification>

<success_criteria>
AUTH-03: database enforces per-user profile row access under Clerk-shaped JWT; schema applied via [BLOCKING] push.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-03-SUMMARY.md`
</output>
