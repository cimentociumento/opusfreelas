---
phase: 01-foundation-identity
plan: 05
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(auth)/sign-in.tsx
  - apps/mobile/app/index.tsx
  - apps/mobile/lib/supabase.ts
  - apps/mobile/.env.example
  - apps/mobile/README.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup:
  - service: clerk
    why: "Phone OTP (AUTH-01) and sessions (AUTH-02)"
    env_vars:
      - name: EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY
        source: "Clerk publishable key"
    dashboard_config:
      - task: "SMS phone; BR +55 allowlist; backup email (D-02)"
        location: "Clerk authentication settings"
  - service: supabase
    env_vars:
      - name: EXPO_PUBLIC_SUPABASE_URL
      - name: EXPO_PUBLIC_SUPABASE_ANON_KEY
must_haves:
  truths:
    - "Phone OTP custom flow UI exists (AUTH-01)"
    - "Clerk token cache uses expo-secure-store pattern (AUTH-02)"
    - "Supabase createClient uses async Clerk accessToken"
  artifacts:
    - path: apps/mobile/app/(auth)/sign-in.tsx
      provides: "OTP UI"
    - path: apps/mobile/lib/supabase.ts
      provides: "Supabase + Clerk JWT"
    - path: apps/mobile/app/_layout.tsx
      provides: "ClerkProvider"
  key_links:
    - from: apps/mobile/lib/supabase.ts
      to: Clerk getToken
      via: accessToken callback
---

<objective>
Integrate **@clerk/clerk-expo** (phone OTP **AUTH-01**), **expo-secure-store** token cache (**AUTH-02**), and **Supabase** client with Clerk JWT for RLS.

Purpose: Roadmap criteria 1–2; D-03 single account.
Output: Auth screen, ClerkProvider, `.env.example`, README notes for BR SMS and D-04 session policy.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation-identity/01-RESEARCH.md

**Versions:** `@clerk/clerk-expo@^2.19.0`, `expo-secure-store` via `expo install`, `@supabase/supabase-js@^2.49.0`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClerkProvider + SecureStore + env template</name>
  <read_first>
    - https://docs.expo.dev/guides/using-clerk/
    - apps/mobile/app/_layout.tsx
  </read_first>
  <files>apps/mobile/package.json, apps/mobile/app/_layout.tsx, apps/mobile/.env.example, apps/mobile/README.md</files>
  <action>
    Add `@clerk/clerk-expo`, `expo-secure-store`, `@supabase/supabase-js`. Wrap root in `ClerkProvider` with **tokenCache** from SecureStore per Expo guide. `EXPO_PUBLIC_*` in `.env.example`. README: D-04 24h session target in Clerk Dashboard; D-03 comment in layout.
  </action>
  <acceptance_criteria>
    - `rg -n "ClerkProvider|SecureStore|tokenCache" apps/mobile/app/_layout.tsx` matches
    - `rg -n "EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY" apps/mobile/.env.example` matches
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; rg -n "ClerkProvider" apps/mobile/app/_layout.tsx</automated>
  </verify>
  <done>Clerk + SecureStore wired</done>
</task>

<task type="auto">
  <name>Task 2: Phone OTP sign-in (custom flow)</name>
  <read_first>
    - https://clerk.dev/docs/guides/development/custom-flows/authentication/email-sms-otp
  </read_first>
  <files>apps/mobile/app/(auth)/sign-in.tsx, apps/mobile/app/index.tsx</files>
  <action>
    Implement E.164 `+55` phone input, `useSignIn` / `useSignUp`, `prepareFirstFactor` / `attemptFirstFactor` with `phone_code`. Navigate signed-in users to home. Update `index.tsx` to route to sign-in when signed out.
  </action>
  <acceptance_criteria>
    - `rg -n "useSignIn|phone_code|prepareFirstFactor" apps/mobile/app/(auth)/sign-in.tsx` matches
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; rg -n "prepareFirstFactor" "apps/mobile/app/(auth)/sign-in.tsx"</automated>
  </verify>
  <done>AUTH-01 UI implemented</done>
</task>

<task type="auto">
  <name>Task 3: Supabase client + Clerk accessToken</name>
  <read_first>
    - https://supabase.com/docs/guides/auth/third-party/clerk
  </read_first>
  <files>apps/mobile/lib/supabase.ts</files>
  <action>
    Export Supabase `createClient` with `accessToken: async () => (await getToken()) ?? null` using Clerk `useAuth().getToken` via a documented factory/hook pattern to avoid invalid hook usage in module scope.
  </action>
  <acceptance_criteria>
    - `rg -n "createClient|accessToken" apps/mobile/lib/supabase.ts` matches
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; rg -n "accessToken" apps/mobile/lib/supabase.ts</automated>
  </verify>
  <done>Supabase sees Clerk JWT for RLS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device | Secrets in SecureStore not AsyncStorage |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | OTP abuse | transfer | Clerk rate limits; BR allowlist doc |
| T-05-02 | E | Client-only authz | mitigate | API enforces AUTH-03 (01-04) |
</threat_model>

<verification>
Grep hooks; manual OTP per 01-VALIDATION.md for BR.
</verification>

<success_criteria>
OTP flow present; SecureStore cache; Supabase wired with Clerk token.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-05-SUMMARY.md`
</output>
