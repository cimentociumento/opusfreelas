---
phase: 01-foundation-identity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - packages/shared/package.json
  - packages/shared/src/index.ts
  - packages/shared/src/roles.ts
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(auth)/sign-in.tsx
  - apps/mobile/app/(auth)/verify.tsx
  - apps/mobile/app/(app)/_layout.tsx
  - apps/mobile/app/(app)/index.tsx
  - apps/mobile/.env.example
  - apps/api/package.json
  - apps/api/src/index.ts
  - apps/api/vitest.config.ts
  - apps/api/src/health.test.ts
  - .github/workflows/ci.yml
  - README.md
autonomous: true
requirements:
  - AUTH-01
user_setup:
  - service: clerk
    why: "Phone OTP and JWT issuance (AUTH-01, D-01)"
    env_vars:
      - name: EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard → API Keys → Publishable key"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard → API Keys → Secret key (for future API; document only in phase 1 if unused)"
    dashboard_config:
      - task: "Enable Phone number strategy and add Brazil (+55) to SMS allowlist"
        location: "Clerk Dashboard → User & Authentication → Phone / SMS"
must_haves:
  truths:
    - "User can start phone OTP sign-in or sign-up from the mobile app using E.164 input"
    - "CI runs install, typecheck/build, and API unit tests on push"
    - "Shared package exports role types used by later phases"
  artifacts:
    - path: "apps/mobile/app/(auth)/sign-in.tsx"
      provides: "Phone identifier + OTP trigger UI wired to Clerk custom flow"
    - path: "apps/api/src/index.ts"
      provides: "Hono server with GET /health"
    - path: "packages/shared/src/roles.ts"
      provides: "Zod role enum / profile role flags contract"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "ClerkProvider"
      via: "wraps app; EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY"
      pattern: "ClerkProvider"
---

<objective>
Bootstrap the pnpm + Turborepo monorepo (D-09), add the Expo SDK 55 mobile shell with Clerk phone OTP custom flow (AUTH-01; D-01, D-03), and a minimal Hono API with Vitest + GitHub Actions CI so later plans can extend the same packages without file conflicts.

Purpose: Unblock parallel work on session policy and database authorization in subsequent waves.
Output: Runnable `pnpm install`, `apps/mobile` with auth routes, `apps/api` health + tests, `packages/shared` role contracts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-identity/01-CONTEXT.md
@.planning/phases/01-foundation-identity/01-RESEARCH.md
@AGENTS.md

**Locked decisions implemented here:** D-01 (Clerk + JWT + Supabase/Firebase stack baseline — Clerk + Expo wired; Supabase in later plan), D-03 (single account — one Clerk user), D-08 seeds RPC-style API naming in `apps/api` README convention), D-09 (local + CI).

**Version pins (concrete):** `expo@~55.0.0`, `expo-router` compatible with SDK 55, `@clerk/clerk-expo@^2.19.31`, `hono@^4.12.0`, `vitest@^4.1.4`, `zod@^3.23.0` (align with AGENTS.md STACK; do not introduce zod 4 unless monorepo-wide decision exists).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monorepo root and shared role contracts</name>
  <read_first>
    - .planning/phases/01-foundation-identity/01-CONTEXT.md
    - .planning/phases/01-foundation-identity/01-RESEARCH.md
    - AGENTS.md
  </read_first>
  <files>package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json, packages/shared/package.json, packages/shared/src/index.ts, packages/shared/src/roles.ts, README.md</files>
  <action>
    Create a pnpm workspace at repo root with `apps/*` and `packages/*`. Set `packageManager` to `pnpm@9.x` and `engines.node` to `>=22`. Add Turborepo `turbo.json` with pipelines for `build`, `lint`, `test`. In `packages/shared`, export Zod schemas and TypeScript types for profile roles per D-06/D-03: at minimum `contractor` and `provider` booleans or an explicit discriminated model—document in `roles.ts` that one user may enable both. Export from `packages/shared/src/index.ts`. Root `README.md` must document `pnpm install` and `pnpm turbo run test` entrypoints (D-09).
  </action>
  <verify>
    <automated>pnpm install && pnpm exec tsc -p packages/shared --noEmit</automated>
  </verify>
  <done>Workspace installs cleanly; `packages/shared` compiles; `roles.ts` exports Zod-validated role shape referenced by later plans.</done>
  <acceptance_criteria>
    - `grep -q "packages/shared" pnpm-workspace.yaml` succeeds
    - `grep -q "contractor" packages/shared/src/roles.ts` succeeds
    - `test -f turbo.json` (or PowerShell `Test-Path turbo.json`) is true
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Expo app with Clerk phone OTP custom flow</name>
  <read_first>
    - https://docs.expo.dev/guides/using-clerk/ (external; Clerk Expo guide)
    - .planning/phases/01-foundation-identity/01-RESEARCH.md
  </read_first>
  <files>apps/mobile/package.json, apps/mobile/app.json, apps/mobile/app/_layout.tsx, apps/mobile/app/(auth)/sign-in.tsx, apps/mobile/app/(auth)/verify.tsx, apps/mobile/app/(app)/_layout.tsx, apps/mobile/app/(app)/index.tsx, apps/mobile/.env.example</files>
  <action>
    Scaffold `apps/mobile` with **Expo SDK 55** (`expo@~55.0.0`), `expo-router`, `react@19.x` / `react-native@0.83.x` as required by the template. Install `npx expo install @clerk/clerk-expo expo-secure-store`. Wrap the root layout with `ClerkProvider` using `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY`. Implement **phone OTP** using Clerk **custom flow** (`useSignIn` / `useSignUp`) per Clerk email/SMS OTP docs: screen `(auth)/sign-in` collects **E.164** phone (document +55 for Brazil), calls `signIn.create` / `signUp.create` with identifier; `(auth)/verify` submits the SMS code via `prepareFirstFactor` / `attemptFirstFactor` (or equivalent documented Clerk Expo APIs). On success, redirect to `(app)` group. Add `.env.example` listing `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY`. Document in `apps/mobile/README.md` that Clerk Dashboard must allow **Brazil SMS** (AUTH-01 pitfall). Do **not** add a second IdP (D-01): Firebase remains unused in code until an explicit later task adds FCM/config placeholders.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Typecheck passes; auth screens exist and call Clerk hooks; E.164 noted for BR; no raw SMS provider hand-roll.</done>
  <acceptance_criteria>
    - `grep -r "useSignIn\\|useSignUp" apps/mobile/app` matches at least one file
    - `grep -q "EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY" apps/mobile/.env.example`
    - `grep -q "ClerkProvider" apps/mobile/app/_layout.tsx`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Minimal Hono API, Vitest, CI workflow</name>
  <read_first>
    - .planning/phases/01-foundation-identity/01-RESEARCH.md
  </read_first>
  <files>apps/api/package.json, apps/api/src/index.ts, apps/api/vitest.config.ts, apps/api/src/health.test.ts, .github/workflows/ci.yml</files>
  <action>
    Create `apps/api` with `hono`, `@hono/node-server` (or documented Hono Node adapter), TypeScript, and `vitest@^4.1.4`. Expose `GET /health` returning JSON `{ "ok": true }`. Add `apps/api/src/health.test.ts` asserting status 200. Add `.github/workflows/ci.yml` on push/PR: setup pnpm 9, Node 22, `pnpm install --frozen-lockfile`, `pnpm turbo run test --filter=api` (or equivalent filter so only scaffolded packages run). Ensure workflow installs from repo root. This establishes D-09 repeatable CI; D-10 full observability is extended in plan `01-04`.
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run</automated>
  </verify>
  <done>CI workflow file exists; Vitest passes for health check; lockfile committed if generated.</done>
  <acceptance_criteria>
    - `grep -q "/health" apps/api/src/index.ts`
    - `grep -q "vitest" apps/api/package.json`
    - `test -f .github/workflows/ci.yml`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Clerk | Phone OTP and SMS delivery handled by Clerk; app never sees SMS gateway secrets |
| Client → API (future) | JWT bearer; API must verify signature and `authorizedParties` in plan 01-04 |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Fake mobile client | mitigate | Clerk publishable key is public by design; session JWT issuance only after OTP verified by Clerk |
| T-01-02 | Information disclosure | OTP / tokens in logs | mitigate | No `console.log` of codes or JWT in committed code; CI must not print secrets |
| T-01-03 | Tampering | User edits role in UI only | mitigate | No trusted role write from client in this plan; roles enforced server-side in 01-04 (D-07) |
| T-01-04 | Denial of service | CI resource abuse | accept | Public repo standard; rate limits via GitHub |

</threat_model>

<verification>
pnpm install; pnpm turbo run test; mobile `tsc --noEmit`; Clerk BR SMS allowlist documented for human UAT later.
</verification>

<success_criteria>
AUTH-01 satisfied at code level: phone OTP flow implemented with Clerk custom flow and E.164; CI green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-01-SUMMARY.md`
</output>
