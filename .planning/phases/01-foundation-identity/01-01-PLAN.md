---
phase: 01-foundation-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - turbo.json
  - tsconfig.base.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/identity/roles.ts
  - packages/shared/src/identity/schemas.ts
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/vitest.config.ts
  - apps/api/src/index.ts
  - apps/api/src/health.test.ts
autonomous: true
requirements:
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - "Monorepo installs with pnpm and runs API unit tests in under 120 seconds"
    - "Shared package exports role types and Zod schemas used by later authz work"
  artifacts:
    - path: packages/shared/src/identity/roles.ts
      provides: "Contractor/provider role model per D-06"
      min_lines: 15
    - path: apps/api/vitest.config.ts
      provides: "Vitest entrypoint for API tests"
    - path: apps/api/src/health.test.ts
      provides: "Sample passing test proving test wiring"
  key_links:
    - from: packages/shared/src/index.ts
      to: apps/api
      via: "workspace dependency @amauc/shared"
---

<objective>
Bootstrap the pnpm + Turborepo monorepo with `packages/shared` (identity contracts for RBAC) and a minimal `apps/api` Hono service with Vitest wired, so downstream plans can implement Clerk JWT verification and role checks without ad-hoc types.

Purpose: Greenfield repeatable baseline (D-09 foundation) and explicit authorization contracts (AUTH-03, D-06, D-07 server-side focus).
Output: Root workspace, shared Zod/role exports, API dev server script, passing Vitest smoke test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-identity/01-CONTEXT.md
@.planning/phases/01-foundation-identity/01-RESEARCH.md
@AGENTS.md

**Pinned versions:** Node **22.x**, **Hono 4.x**, **Zod 3.x** (`zod@^3.24.0` per AGENTS.md STACK).

**Package naming:** `packages/shared` → `"name": "@amauc/shared"`; `apps/api` → `"name": "@amauc/api"`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root workspace (pnpm + Turborepo)</name>
  <read_first>
    - AGENTS.md (pnpm, Turborepo)
    - .planning/phases/01-foundation-identity/01-RESEARCH.md (Recommended Project Structure)
  </read_first>
  <files>package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json</files>
  <action>
    Create repository root with **pnpm** `packageManager` field (pnpm 9.x), `pnpm-workspace.yaml` including `apps/*` and `packages/*`, and **Turborepo** `turbo.json` with pipeline tasks `build` and `test`. Add root `tsconfig.base.json` with `strict: true`. Root scripts: `"test": "turbo run test"`. Do **not** create `apps/mobile` here (plan 01-02). Do **not** add `.github/workflows` here (plan 01-06).
  </action>
  <acceptance_criteria>
    - `pnpm install` completes at repo root without error
    - File `pnpm-workspace.yaml` contains both `packages/*` and `apps/*`
    - `node -p "require('./package.json').packageManager"` starts with `pnpm@`
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; pnpm install; pnpm -v</automated>
  </verify>
  <done>Workspace resolves; turbo is callable</done>
</task>

<task type="auto">
  <name>Task 2: packages/shared — identity roles + Zod contracts</name>
  <read_first>
    - .planning/phases/01-foundation-identity/01-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <files>packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts, packages/shared/src/identity/roles.ts, packages/shared/src/identity/schemas.ts</files>
  <action>
    Add `packages/shared` with `"name": "@amauc/shared"`. Export: (1) types/const for **contractor** and **provider** with both-true allowed per **D-06**; (2) Zod schema for profile role flags; (3) pure function `assertCanActAs(context, 'contractor' | 'provider')` for API use (**AUTH-03**). Dependency: `zod@^3.24.0`. Add `tsc` build or `ts` consumed via project references — one consistent pattern.
  </action>
  <acceptance_criteria>
    - `rg -n "isContractor|isProvider|assertCanActAs" packages/shared/src/identity/` finds matches
    - `pnpm --filter @amauc/shared exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; pnpm --filter @amauc/shared exec tsc --noEmit</automated>
  </verify>
  <done>Shared package typechecks; exports are importable</done>
</task>

<task type="auto">
  <name>Task 3: apps/api — Hono skeleton + Vitest smoke test</name>
  <read_first>
    - packages/shared/src/index.ts
  </read_first>
  <files>apps/api/package.json, apps/api/tsconfig.json, apps/api/vitest.config.ts, apps/api/src/index.ts, apps/api/src/health.test.ts</files>
  <action>
    Create `apps/api` with **hono@^4.12.0**, **typescript**, **vitest@^2.1.0**, **tsx** (or **ts-node**), **@types/node**. Minimal `src/index.ts` exporting Hono app with `GET /health` → `{ ok: true }`. `health.test.ts` requests `/health` via `app.request`. Add workspace dependency on `@amauc/shared` and import `assertCanActAs` in test with valid context. Scripts: `"test": "vitest run"`, `"dev": "tsx watch src/index.ts"`. Package name `@amauc/api`.
  </action>
  <acceptance_criteria>
    - `rg -n "new Hono|/health" apps/api/src/index.ts` matches
    - `cd apps/api; pnpm vitest run` exits 0
    - `apps/api/package.json` lists `"@amauc/shared": "workspace:*"` (or equivalent)
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas\apps\api; pnpm vitest run</automated>
  </verify>
  <done>Health route test passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workspace → secrets | No `.env` with real keys committed; only later `.env.example` |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I | Shared role helpers | mitigate | Pure functions + Zod; server re-validates later |
| T-01-02 | T | Dependency supply chain | mitigate | Pin majors; commit `pnpm-lock.yaml` |
| T-01-03 | E | Misconfigured workspace | accept | Documented greps; CI in plan 01-06 |
</threat_model>

<verification>
`pnpm install` at root; `pnpm vitest run` in `apps/api`.
</verification>

<success_criteria>
Monorepo installs; `@amauc/shared` exports contracts; `@amauc/api` `/health` tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-01-SUMMARY.md`
</output>
