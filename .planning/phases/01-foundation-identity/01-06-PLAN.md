---
phase: 01-foundation-identity
plan: 06
type: execute
wave: 4
depends_on:
  - "01-04"
  - "01-05"
files_modified:
  - .github/workflows/ci.yml
  - apps/api/package.json
  - apps/api/src/observability.ts
  - apps/api/src/index.ts
  - apps/mobile/app.json
  - docs/runbooks/observability.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "CI workflow runs pnpm install and API vitest on Node 22"
    - "API registers pino logging and @hono/otel middleware (D-10)"
    - "Firebase placeholder in Expo config — no Firebase Auth (D-01 support)"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "CI"
    - path: apps/api/src/observability.ts
      provides: "OTel + logs"
    - path: docs/runbooks/observability.md
      provides: "Ops notes"
  key_links:
    - from: apps/api/src/index.ts
      to: apps/api/src/observability.ts
      via: initObservability
---

<objective>
Add **GitHub Actions** (D-09), **pino** + **@hono/otel** (D-10), and **Firebase** `expo.extra` placeholders without Firebase Auth (D-01 “suporte”).

Purpose: Repeatable CI (D-09) and observability baseline (D-10). **AUTH-02** (SecureStore/session UX) is delivered in **01-05**; manual/Maestro proof stays per `01-VALIDATION.md`.
Output: `ci.yml`, observability module, runbook, Firebase comment-only stub.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation-identity/01-RESEARCH.md

Node **22**, pnpm **9**, `@hono/otel@^1.1.0`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI</name>
  <read_first>
    - package.json
    - apps/api/package.json
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Workflow: checkout, `pnpm/action-setup@v4` v9, `actions/setup-node@v4` node 22 `cache: pnpm`, `pnpm install --frozen-lockfile`, `pnpm turbo run test --filter=@amauc/api` or `pnpm --filter @amauc/api vitest run`.
  </action>
  <acceptance_criteria>
    - `rg -n "pnpm install|vitest|22" .github/workflows/ci.yml` matches
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; rg -n "vitest|pnpm" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI file present</done>
</task>

<task type="auto">
  <name>Task 2: observability.ts — pino + @hono/otel</name>
  <read_first>
    - apps/api/src/index.ts
  </read_first>
  <files>apps/api/package.json, apps/api/src/observability.ts, apps/api/src/index.ts, docs/runbooks/observability.md</files>
  <action>
    Add `pino`, `@hono/otel`, optional `@opentelemetry/sdk-node`. `observability.ts` exports `initObservability(app)` registering OTel middleware + structured logger. `index.ts` calls it. Document `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_SERVICE_NAME=amauc-api`. No PII in log fields.
  </action>
  <acceptance_criteria>
    - `rg -n "initObservability|@hono/otel|pino" apps/api/src/observability.ts apps/api/src/index.ts` matches
    - `pnpm vitest run` in apps/api exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas\apps\api; pnpm vitest run</automated>
  </verify>
  <done>D-10 baseline on API</done>
</task>

<task type="auto">
  <name>Task 3: Firebase placeholder (D-01)</name>
  <read_first>
    - apps/mobile/app.json
  </read_first>
  <files>apps/mobile/app.json</files>
  <action>
    Add `expo.extra` keys `firebaseProjectId` empty string and comment: Firebase for FCM/Analytics later — **Clerk is sole IdP**. Do not add `firebase` auth packages.
  </action>
  <acceptance_criteria>
    - `rg -n "firebase|IdP|Clerk" apps/mobile/app.json` matches
  </acceptance_criteria>
  <verify>
    <automated>cd c:\opusfreelas; rg -n "firebase|IdP" apps/mobile/app.json</automated>
  </verify>
  <done>Stub documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI | Third-party actions; pin major versions |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | Workflow injection | mitigate | Minimal permissions; standard GHA |
| T-06-02 | I | Logs | mitigate | No phone numbers in structured logs |
</threat_model>

<verification>
`pnpm vitest run` in `apps/api`; optional workflow `yaml` lint.
</verification>

<success_criteria>
CI + observability + Firebase placeholder complete; API tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-06-SUMMARY.md`
</output>
