# Plan: 03-03 — Backend Discovery RPC

## Goal
Implement the RPC handler for searching providers.

## Context
- Connects the API to the `search_providers` SQL RPC.
- Returns a formatted list of providers for the mobile app.

## Proposed Changes

### 1. New File: `apps/api/src/rpc/discovery.ts`
- Implement `discovery.searchProviders` handler.
- Validate input using `providerSearchSchema`.
- Call Supabase RPC `search_providers`.
- Map database results to `ProviderResult` type.

### 2. Update `apps/api/src/rpc/router.ts`
- Import and merge `discoveryHandlers`.

### 3. Update `apps/api/src/rpc/identity.ts`
- Add `identity.updateProviderProfile` to allow providers to set their location and categories.
- This is essential for testing Phase 3.

## Verification
- [ ] Call `discovery.searchProviders` via HTTP and verify JSON response.
- [ ] Verify that only providers are returned.
- [ ] Verify that distance is calculated and returned.
