{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'no build step needed: the /api function bundles its own TypeScript, and @amauc/shared is consumed straight from src/ (see its package.json exports) — never from a compiled dist'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
